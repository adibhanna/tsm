[tools]
go = "1.25"

[tasks.build]
description = "Build tsm binary"
run = "go build -o tsm ."

[tasks.install]
description = "Install tsm to $GOPATH/bin"
run = "go install ."

[tasks.fmt]
description = "Format code"
run = "gofmt -w ."

[tasks.lint]
description = "Run linter"
run = "go vet ./..."

[tasks.test]
description = "Run tests"
run = "go test ./..."

[tasks.clean]
description = "Remove built binary"
run = "rm -f tsm"
